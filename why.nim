import nimib

template why*: untyped =
  slide:
    nbText: "## why use nim for embedded"
    slide:
      listItem: nbText: "modern tooling (story is getting better with nim 3)"
      listItem: nbText: "safe high-level language, with access to unsafe low-level stuff"
      listItem: nbText: "can be used for real time stuff"
      listItem: nbText: "compiles to C - enables nim on virtually every platform"
