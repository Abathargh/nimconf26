## nimconf26 - nim for embedded software development

This project generates the slides for my nimconf26 talk about using nim for 
embedded development.

Generate them as such:

```bash
atlas init && atlas install
nim c nimconf26
./nimconf26
```
